<h1>Questions</h1>

<% @questions.each do |question| %>
    <h2><%= question.title %></h2>

    <time datetime="<%= question.created_at %>">
        <%= question.created_at %>
    </time>

    <p>
        <%= question.body %>
    </p>
<% end %>
